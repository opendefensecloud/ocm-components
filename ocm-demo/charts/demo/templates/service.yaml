apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-demo
  labels:
    app: {{ .Release.Name }}-demo
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
  selector:
    app: {{ .Release.Name }}-demo
